export interface TSpecial {
  image: string;
  title: string;
  originalPrice: number;
  discountPrice: number;
  discount: number;
  badge: string;
  badgeColor: string;
  timeLeft: string;
  features: string[];
}
